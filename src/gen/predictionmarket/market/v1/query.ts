// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               v7.34.0
// source: predictionmarket/market/v1/query.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "predictionmarket.market.v1";

export interface QueryParlayLeg {
  marketId: bigint;
  requiredOutcome: string;
}

export interface MarketInfo {
  marketId: bigint;
  question: string;
  metadataUri: string;
  conditionId: string;
  yesPositionId: string;
  noPositionId: string;
  status: string;
  takerFeeBps: number;
  resolutionMetadataUri: string;
  winningOutcome: string;
  type: string;
  negRiskGroupId: bigint;
  parlayLegs: QueryParlayLeg[];
}

export interface PositionInfo {
  positionId: string;
  denom: string;
  collateralDenom: string;
  parentCollectionId: string;
  collectionId: string;
  conditionId: string;
  indexSet: number;
}

export interface NegRiskGroupInfo {
  groupId: bigint;
  title: string;
  metadataUri: string;
  marketIds: bigint[];
}

export interface QueryMarketRequest {
  marketId: bigint;
}

export interface QueryMarketByPositionRequest {
  positionId: string;
}

export interface QueryMarketsRequest {
  offset: bigint;
  limit: number;
}

export interface QueryNegRiskGroupRequest {
  groupId: bigint;
}

export interface QueryMarketResponse {
  market?: MarketInfo | undefined;
  yesPosition?: PositionInfo | undefined;
  noPosition?: PositionInfo | undefined;
  negRiskGroup?: NegRiskGroupInfo | undefined;
}

export interface QueryMarketsResponse {
  markets: MarketInfo[];
  totalCount: bigint;
  offset: bigint;
  limit: number;
}

export interface QueryNegRiskGroupResponse {
  negRiskGroup?: NegRiskGroupInfo | undefined;
  markets: MarketInfo[];
}

function createBaseQueryParlayLeg(): QueryParlayLeg {
  return { marketId: 0n, requiredOutcome: "" };
}

export const QueryParlayLeg: MessageFns<QueryParlayLeg> = {
  encode(message: QueryParlayLeg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.marketId !== 0n) {
      if (BigInt.asUintN(64, message.marketId) !== message.marketId) {
        throw new globalThis.Error("value provided for field message.marketId of type uint64 too large");
      }
      writer.uint32(8).uint64(message.marketId);
    }
    if (message.requiredOutcome !== "") {
      writer.uint32(18).string(message.requiredOutcome);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryParlayLeg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryParlayLeg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.marketId = reader.uint64() as bigint;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.requiredOutcome = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<QueryParlayLeg>, I>>(base?: I): QueryParlayLeg {
    return QueryParlayLeg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryParlayLeg>, I>>(object: I): QueryParlayLeg {
    const message = createBaseQueryParlayLeg();
    message.marketId = object.marketId ?? 0n;
    message.requiredOutcome = object.requiredOutcome ?? "";
    return message;
  },
};

function createBaseMarketInfo(): MarketInfo {
  return {
    marketId: 0n,
    question: "",
    metadataUri: "",
    conditionId: "",
    yesPositionId: "",
    noPositionId: "",
    status: "",
    takerFeeBps: 0,
    resolutionMetadataUri: "",
    winningOutcome: "",
    type: "",
    negRiskGroupId: 0n,
    parlayLegs: [],
  };
}

export const MarketInfo: MessageFns<MarketInfo> = {
  encode(message: MarketInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.marketId !== 0n) {
      if (BigInt.asUintN(64, message.marketId) !== message.marketId) {
        throw new globalThis.Error("value provided for field message.marketId of type uint64 too large");
      }
      writer.uint32(8).uint64(message.marketId);
    }
    if (message.question !== "") {
      writer.uint32(18).string(message.question);
    }
    if (message.metadataUri !== "") {
      writer.uint32(26).string(message.metadataUri);
    }
    if (message.conditionId !== "") {
      writer.uint32(34).string(message.conditionId);
    }
    if (message.yesPositionId !== "") {
      writer.uint32(42).string(message.yesPositionId);
    }
    if (message.noPositionId !== "") {
      writer.uint32(50).string(message.noPositionId);
    }
    if (message.status !== "") {
      writer.uint32(66).string(message.status);
    }
    if (message.takerFeeBps !== 0) {
      writer.uint32(72).uint32(message.takerFeeBps);
    }
    if (message.resolutionMetadataUri !== "") {
      writer.uint32(82).string(message.resolutionMetadataUri);
    }
    if (message.winningOutcome !== "") {
      writer.uint32(90).string(message.winningOutcome);
    }
    if (message.type !== "") {
      writer.uint32(98).string(message.type);
    }
    if (message.negRiskGroupId !== 0n) {
      if (BigInt.asUintN(64, message.negRiskGroupId) !== message.negRiskGroupId) {
        throw new globalThis.Error("value provided for field message.negRiskGroupId of type uint64 too large");
      }
      writer.uint32(104).uint64(message.negRiskGroupId);
    }
    for (const v of message.parlayLegs) {
      QueryParlayLeg.encode(v!, writer.uint32(114).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MarketInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMarketInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.marketId = reader.uint64() as bigint;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.question = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.metadataUri = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.conditionId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.yesPositionId = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.noPositionId = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.takerFeeBps = reader.uint32();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.resolutionMetadataUri = reader.string();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.winningOutcome = reader.string();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.type = reader.string();
          continue;
        }
        case 13: {
          if (tag !== 104) {
            break;
          }

          message.negRiskGroupId = reader.uint64() as bigint;
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.parlayLegs.push(QueryParlayLeg.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<MarketInfo>, I>>(base?: I): MarketInfo {
    return MarketInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MarketInfo>, I>>(object: I): MarketInfo {
    const message = createBaseMarketInfo();
    message.marketId = object.marketId ?? 0n;
    message.question = object.question ?? "";
    message.metadataUri = object.metadataUri ?? "";
    message.conditionId = object.conditionId ?? "";
    message.yesPositionId = object.yesPositionId ?? "";
    message.noPositionId = object.noPositionId ?? "";
    message.status = object.status ?? "";
    message.takerFeeBps = object.takerFeeBps ?? 0;
    message.resolutionMetadataUri = object.resolutionMetadataUri ?? "";
    message.winningOutcome = object.winningOutcome ?? "";
    message.type = object.type ?? "";
    message.negRiskGroupId = object.negRiskGroupId ?? 0n;
    message.parlayLegs = object.parlayLegs?.map((e) => QueryParlayLeg.fromPartial(e)) || [];
    return message;
  },
};

function createBasePositionInfo(): PositionInfo {
  return {
    positionId: "",
    denom: "",
    collateralDenom: "",
    parentCollectionId: "",
    collectionId: "",
    conditionId: "",
    indexSet: 0,
  };
}

export const PositionInfo: MessageFns<PositionInfo> = {
  encode(message: PositionInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.positionId !== "") {
      writer.uint32(10).string(message.positionId);
    }
    if (message.denom !== "") {
      writer.uint32(18).string(message.denom);
    }
    if (message.collateralDenom !== "") {
      writer.uint32(26).string(message.collateralDenom);
    }
    if (message.parentCollectionId !== "") {
      writer.uint32(34).string(message.parentCollectionId);
    }
    if (message.collectionId !== "") {
      writer.uint32(42).string(message.collectionId);
    }
    if (message.conditionId !== "") {
      writer.uint32(50).string(message.conditionId);
    }
    if (message.indexSet !== 0) {
      writer.uint32(56).uint32(message.indexSet);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PositionInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePositionInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.positionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.denom = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.collateralDenom = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.parentCollectionId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.collectionId = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.conditionId = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.indexSet = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<PositionInfo>, I>>(base?: I): PositionInfo {
    return PositionInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PositionInfo>, I>>(object: I): PositionInfo {
    const message = createBasePositionInfo();
    message.positionId = object.positionId ?? "";
    message.denom = object.denom ?? "";
    message.collateralDenom = object.collateralDenom ?? "";
    message.parentCollectionId = object.parentCollectionId ?? "";
    message.collectionId = object.collectionId ?? "";
    message.conditionId = object.conditionId ?? "";
    message.indexSet = object.indexSet ?? 0;
    return message;
  },
};

function createBaseNegRiskGroupInfo(): NegRiskGroupInfo {
  return { groupId: 0n, title: "", metadataUri: "", marketIds: [] };
}

export const NegRiskGroupInfo: MessageFns<NegRiskGroupInfo> = {
  encode(message: NegRiskGroupInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.groupId !== 0n) {
      if (BigInt.asUintN(64, message.groupId) !== message.groupId) {
        throw new globalThis.Error("value provided for field message.groupId of type uint64 too large");
      }
      writer.uint32(8).uint64(message.groupId);
    }
    if (message.title !== "") {
      writer.uint32(18).string(message.title);
    }
    if (message.metadataUri !== "") {
      writer.uint32(26).string(message.metadataUri);
    }
    writer.uint32(34).fork();
    for (const v of message.marketIds) {
      if (BigInt.asUintN(64, v) !== v) {
        throw new globalThis.Error("a value provided in array field marketIds of type uint64 is too large");
      }
      writer.uint64(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NegRiskGroupInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNegRiskGroupInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.groupId = reader.uint64() as bigint;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.metadataUri = reader.string();
          continue;
        }
        case 4: {
          if (tag === 32) {
            message.marketIds.push(reader.uint64() as bigint);

            continue;
          }

          if (tag === 34) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.marketIds.push(reader.uint64() as bigint);
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<NegRiskGroupInfo>, I>>(base?: I): NegRiskGroupInfo {
    return NegRiskGroupInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<NegRiskGroupInfo>, I>>(object: I): NegRiskGroupInfo {
    const message = createBaseNegRiskGroupInfo();
    message.groupId = object.groupId ?? 0n;
    message.title = object.title ?? "";
    message.metadataUri = object.metadataUri ?? "";
    message.marketIds = object.marketIds?.map((e) => e) || [];
    return message;
  },
};

function createBaseQueryMarketRequest(): QueryMarketRequest {
  return { marketId: 0n };
}

export const QueryMarketRequest: MessageFns<QueryMarketRequest> = {
  encode(message: QueryMarketRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.marketId !== 0n) {
      if (BigInt.asUintN(64, message.marketId) !== message.marketId) {
        throw new globalThis.Error("value provided for field message.marketId of type uint64 too large");
      }
      writer.uint32(8).uint64(message.marketId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryMarketRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryMarketRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.marketId = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<QueryMarketRequest>, I>>(base?: I): QueryMarketRequest {
    return QueryMarketRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryMarketRequest>, I>>(object: I): QueryMarketRequest {
    const message = createBaseQueryMarketRequest();
    message.marketId = object.marketId ?? 0n;
    return message;
  },
};

function createBaseQueryMarketByPositionRequest(): QueryMarketByPositionRequest {
  return { positionId: "" };
}

export const QueryMarketByPositionRequest: MessageFns<QueryMarketByPositionRequest> = {
  encode(message: QueryMarketByPositionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.positionId !== "") {
      writer.uint32(10).string(message.positionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryMarketByPositionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryMarketByPositionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.positionId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<QueryMarketByPositionRequest>, I>>(base?: I): QueryMarketByPositionRequest {
    return QueryMarketByPositionRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryMarketByPositionRequest>, I>>(object: I): QueryMarketByPositionRequest {
    const message = createBaseQueryMarketByPositionRequest();
    message.positionId = object.positionId ?? "";
    return message;
  },
};

function createBaseQueryMarketsRequest(): QueryMarketsRequest {
  return { offset: 0n, limit: 0 };
}

export const QueryMarketsRequest: MessageFns<QueryMarketsRequest> = {
  encode(message: QueryMarketsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.offset !== 0n) {
      if (BigInt.asUintN(64, message.offset) !== message.offset) {
        throw new globalThis.Error("value provided for field message.offset of type uint64 too large");
      }
      writer.uint32(8).uint64(message.offset);
    }
    if (message.limit !== 0) {
      writer.uint32(16).uint32(message.limit);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryMarketsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryMarketsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.offset = reader.uint64() as bigint;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.limit = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<QueryMarketsRequest>, I>>(base?: I): QueryMarketsRequest {
    return QueryMarketsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryMarketsRequest>, I>>(object: I): QueryMarketsRequest {
    const message = createBaseQueryMarketsRequest();
    message.offset = object.offset ?? 0n;
    message.limit = object.limit ?? 0;
    return message;
  },
};

function createBaseQueryNegRiskGroupRequest(): QueryNegRiskGroupRequest {
  return { groupId: 0n };
}

export const QueryNegRiskGroupRequest: MessageFns<QueryNegRiskGroupRequest> = {
  encode(message: QueryNegRiskGroupRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.groupId !== 0n) {
      if (BigInt.asUintN(64, message.groupId) !== message.groupId) {
        throw new globalThis.Error("value provided for field message.groupId of type uint64 too large");
      }
      writer.uint32(8).uint64(message.groupId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryNegRiskGroupRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryNegRiskGroupRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.groupId = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<QueryNegRiskGroupRequest>, I>>(base?: I): QueryNegRiskGroupRequest {
    return QueryNegRiskGroupRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryNegRiskGroupRequest>, I>>(object: I): QueryNegRiskGroupRequest {
    const message = createBaseQueryNegRiskGroupRequest();
    message.groupId = object.groupId ?? 0n;
    return message;
  },
};

function createBaseQueryMarketResponse(): QueryMarketResponse {
  return { market: undefined, yesPosition: undefined, noPosition: undefined, negRiskGroup: undefined };
}

export const QueryMarketResponse: MessageFns<QueryMarketResponse> = {
  encode(message: QueryMarketResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.market !== undefined) {
      MarketInfo.encode(message.market, writer.uint32(10).fork()).join();
    }
    if (message.yesPosition !== undefined) {
      PositionInfo.encode(message.yesPosition, writer.uint32(18).fork()).join();
    }
    if (message.noPosition !== undefined) {
      PositionInfo.encode(message.noPosition, writer.uint32(26).fork()).join();
    }
    if (message.negRiskGroup !== undefined) {
      NegRiskGroupInfo.encode(message.negRiskGroup, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryMarketResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryMarketResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.market = MarketInfo.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.yesPosition = PositionInfo.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.noPosition = PositionInfo.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.negRiskGroup = NegRiskGroupInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<QueryMarketResponse>, I>>(base?: I): QueryMarketResponse {
    return QueryMarketResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryMarketResponse>, I>>(object: I): QueryMarketResponse {
    const message = createBaseQueryMarketResponse();
    message.market = (object.market !== undefined && object.market !== null)
      ? MarketInfo.fromPartial(object.market)
      : undefined;
    message.yesPosition = (object.yesPosition !== undefined && object.yesPosition !== null)
      ? PositionInfo.fromPartial(object.yesPosition)
      : undefined;
    message.noPosition = (object.noPosition !== undefined && object.noPosition !== null)
      ? PositionInfo.fromPartial(object.noPosition)
      : undefined;
    message.negRiskGroup = (object.negRiskGroup !== undefined && object.negRiskGroup !== null)
      ? NegRiskGroupInfo.fromPartial(object.negRiskGroup)
      : undefined;
    return message;
  },
};

function createBaseQueryMarketsResponse(): QueryMarketsResponse {
  return { markets: [], totalCount: 0n, offset: 0n, limit: 0 };
}

export const QueryMarketsResponse: MessageFns<QueryMarketsResponse> = {
  encode(message: QueryMarketsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.markets) {
      MarketInfo.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.totalCount !== 0n) {
      if (BigInt.asUintN(64, message.totalCount) !== message.totalCount) {
        throw new globalThis.Error("value provided for field message.totalCount of type uint64 too large");
      }
      writer.uint32(16).uint64(message.totalCount);
    }
    if (message.offset !== 0n) {
      if (BigInt.asUintN(64, message.offset) !== message.offset) {
        throw new globalThis.Error("value provided for field message.offset of type uint64 too large");
      }
      writer.uint32(24).uint64(message.offset);
    }
    if (message.limit !== 0) {
      writer.uint32(32).uint32(message.limit);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryMarketsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryMarketsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.markets.push(MarketInfo.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.totalCount = reader.uint64() as bigint;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.offset = reader.uint64() as bigint;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.limit = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<QueryMarketsResponse>, I>>(base?: I): QueryMarketsResponse {
    return QueryMarketsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryMarketsResponse>, I>>(object: I): QueryMarketsResponse {
    const message = createBaseQueryMarketsResponse();
    message.markets = object.markets?.map((e) => MarketInfo.fromPartial(e)) || [];
    message.totalCount = object.totalCount ?? 0n;
    message.offset = object.offset ?? 0n;
    message.limit = object.limit ?? 0;
    return message;
  },
};

function createBaseQueryNegRiskGroupResponse(): QueryNegRiskGroupResponse {
  return { negRiskGroup: undefined, markets: [] };
}

export const QueryNegRiskGroupResponse: MessageFns<QueryNegRiskGroupResponse> = {
  encode(message: QueryNegRiskGroupResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.negRiskGroup !== undefined) {
      NegRiskGroupInfo.encode(message.negRiskGroup, writer.uint32(10).fork()).join();
    }
    for (const v of message.markets) {
      MarketInfo.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryNegRiskGroupResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryNegRiskGroupResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.negRiskGroup = NegRiskGroupInfo.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.markets.push(MarketInfo.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<QueryNegRiskGroupResponse>, I>>(base?: I): QueryNegRiskGroupResponse {
    return QueryNegRiskGroupResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryNegRiskGroupResponse>, I>>(object: I): QueryNegRiskGroupResponse {
    const message = createBaseQueryNegRiskGroupResponse();
    message.negRiskGroup = (object.negRiskGroup !== undefined && object.negRiskGroup !== null)
      ? NegRiskGroupInfo.fromPartial(object.negRiskGroup)
      : undefined;
    message.markets = object.markets?.map((e) => MarketInfo.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
